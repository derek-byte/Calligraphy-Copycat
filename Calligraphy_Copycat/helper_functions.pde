

float findMaxScore(DrawingBoard template, DrawingBoard drawn) {
  
  float score = 0;
  return score;
}

int findOverlappingPixels(PVector[] template, DrawingBoard drawn) {
  int overlap = 0;
 
  return overlap;
}

void handleSubmit() {
  board.uploadData();
}
